import sys

name = sys.argv[1]      # First command-line argument (after script name)
rollno = sys.argv[2]    # Second command-line argument

print(f"Name: {name}\n")        # Prints the name with a newline
print(f"Roll No : {rollno}")    # Prints the roll number
